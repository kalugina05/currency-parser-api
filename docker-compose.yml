version: '3.8'

services:
  nats:
    image: nats:alpine
    ports:
      - "4222:4222"
      - "8222:8222"

  api:
    build: .
    ports:
      - "8000:8000"
    working_dir: /app
    environment:
      - NATS_URL=nats://nats:4222
    command: uvicorn main:app --host 0.0.0.0 --port 8000 --reload
